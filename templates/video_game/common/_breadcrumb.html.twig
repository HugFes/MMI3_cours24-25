<nav class="breadcrumb" aria-label="breadcrumbs">
    <ul>
        {% for item in items %}
        <li><a href="{{ item.href }}" class="{{ item.is_active ? 'is-active' }}">{{ item.label }}</a></li>
        {% endfor %}
    </ul>
</nav>